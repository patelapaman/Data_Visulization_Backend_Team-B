# Feature Selection

## 1. Overview

Feature selection is the process of identifying the most useful features from the processed security-event dataset for machine-learning models.

In the AI-Assisted Threat Detection Dashboard, feature selection helps the ML models focus on security-related information instead of unnecessary columns.

The feature-selection module is implemented in:

```text
ml/feature_selection.py